import { mkdir, writeFile } from 'node:fs/promises';
import { resolve } from 'node:path';
import type { Report } from './types.js';

export const escapeHtml = (s: unknown): string => String(s).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c] ?? c));
const md = (s: string) => s.replace(/[|`<>\r\n]/g, ' ');
export function renderMarkdown(r: Report): string {
  const s = r.summary;
  return `# false-green · ${md(r.suite)}\n\n${s.detected} detected · ${s.survived} survived · ${s.unexercised} not exercised · ${s.unresolved} unresolved\n\n` +
    `Detection rate: ${s.detectionRate === null ? 'not measurable' : s.detectionRate + '%'} (${s.eligible}/${s.total} eligible). Exit code: ${s.exitCode}.\n\n` +
    '| Scenario | Fault | Verdict | Hits per repetition |\n|---|---|---|---|\n' +
    r.scenarios.flatMap(sc => sc.mutations.map(m => `| ${md(sc.id)} | ${md(m.id)} | ${m.verdict} | ${m.trials.map(t => t.hits).join(', ') || 'not run'} |`)).join('\n') +
    '\n\nA detected fault is not proof of product correctness. A survivor needs review for relevance or equivalent behavior. Unexercised and unresolved results never improve the score.\n';
}
export function renderHtml(r: Report): string {
  const e = escapeHtml, s = r.summary;
  const rate = s.detectionRate === null ? '—' : `${s.detectionRate}%`;
  const rows = r.scenarios.map(sc => `<section class="scenario"><div class="scenario-head"><div><p class="eyebrow">SCENARIO / ${e(sc.id)}</p><h2>${e(sc.title)}</h2></div><span class="control ${sc.baselineValid ? 'ok' : 'bad'}">${sc.baselineValid ? 'Clean controls passed' : 'Invalid baseline'}</span></div>
  <div class="table-scroll"><table><thead><tr><th>Injected fault</th><th>Verdict</th><th>Evidence</th></tr></thead><tbody>${sc.mutations.map(m => `<tr><td><strong>${e(m.title)}</strong><small>${e(m.kind)} · ${e(m.id)}</small></td><td><span class="badge ${m.verdict}">${e(m.verdict)}</span></td><td>${m.trials.length ? m.trials.map(t => t.hits).join(' / ') + ' hits' : 'Not run'}</td></tr><tr class="detail"><td colspan="3"><details><summary>Why this result?</summary><p>${e(m.reason)}</p>${m.trials.map((t,i) => `<p class="trial">Run ${i+1} · ${e(t.outcome)} · ${t.durationMs} ms · phase: ${e(t.phase)}${t.message ? '<br>'+e(t.message) : ''}${t.injectionErrors.length ? '<br>'+e(t.injectionErrors.join('; ')) : ''}</p>`).join('')}</details></td></tr>`).join('')}</tbody></table></div>
  <details class="controls"><summary>Inspect clean controls before and after</summary><pre>${e(JSON.stringify({before: sc.baselineBefore, after: sc.baselineAfter}, null, 2))}</pre></details></section>`).join('');
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta http-equiv="Content-Security-Policy" content="default-src 'none'; style-src 'unsafe-inline'; base-uri 'none'; form-action 'none'"><title>false-green | ${e(r.suite)}</title><style>
  :root{color-scheme:dark;--bg:#0b1015;--panel:#111a22;--line:#27353e;--text:#eef4f6;--muted:#b1c2cd;--mint:#8af0c0;--amber:#ffc979}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--text);font:16px/1.6 system-ui,sans-serif}.wrap{max-width:1120px;margin:auto;padding:48px 28px}header{border-bottom:1px solid var(--line);display:flex;justify-content:space-between;padding-bottom:24px;gap:20px}.wordmark{font-size:24px;font-weight:850;letter-spacing:-1px}.wordmark i{font-style:normal;color:var(--mint)}.version{color:var(--muted);font:13px/2 ui-monospace,monospace}.hero{padding:58px 0 35px}.eyebrow{letter-spacing:2px;color:var(--mint);font:11px/1.5 ui-monospace,monospace;margin:0 0 13px}h1{font-size:clamp(38px,5.4vw,64px);line-height:1.08;letter-spacing:-2.5px;margin:0 0 18px;max-width:850px}h1 em{color:var(--mint);font-style:normal}.lead{color:var(--muted);max-width:700px;font-size:18px;margin:0}.metrics{display:grid;grid-template-columns:repeat(4,1fr);gap:14px;margin:30px 0}.metric{border:1px solid var(--line);border-radius:12px;padding:20px;background:var(--panel)}.metric b{display:block;font-size:36px;line-height:1.2;letter-spacing:-1px}.metric span{font-size:12px;color:var(--muted)}.mint{color:var(--mint)}.amber{color:var(--amber)}.notice{border-left:3px solid var(--amber);background:#1c1c1b;padding:15px 20px;font-size:14px;color:#e0dacf}.notice strong{color:var(--amber)}.scenario{border:1px solid var(--line);border-radius:14px;overflow:hidden;margin-top:28px;background:var(--panel)}.scenario-head{padding:24px;display:flex;justify-content:space-between;align-items:center;gap:15px}h2{margin:0;font-size:22px;letter-spacing:-.5px}.scenario-head .eyebrow{margin-bottom:7px}.control,.badge{font:12px/1.6 ui-monospace,monospace;padding:6px 10px;border-radius:5px;white-space:nowrap}.ok,.detected{background:#163a2f;color:#a8f3d0}.bad,.survived{background:#493021;color:#ffd49e}.not-exercised,.inconclusive,.unstable,.baseline-invalid{background:#343047;color:#d5c6ff}.table-scroll{overflow-x:auto}table{width:100%;border-collapse:collapse;min-width:560px}th{text-align:left;color:var(--muted);font-size:11px;text-transform:uppercase;letter-spacing:1px;background:#15202a}td,th{padding:15px 24px;border-top:1px solid var(--line)}td strong{font-size:14px}small{display:block;font:11px/1.8 ui-monospace,monospace;color:var(--muted)}.detail td{padding-top:0;border-top:0;font-size:12px;color:var(--muted)}summary{cursor:pointer;width:fit-content}summary:focus-visible{outline:2px solid var(--mint);outline-offset:4px}.trial{font-family:ui-monospace,monospace;overflow-wrap:anywhere}.controls{padding:14px 24px;border-top:1px solid var(--line);font-size:12px;color:var(--muted)}pre{white-space:pre-wrap;overflow-wrap:anywhere}footer{margin-top:32px;font-size:12px;color:var(--muted);border-top:1px solid var(--line);padding-top:20px}footer p{margin:8px 0}.hash{overflow-wrap:anywhere;font:11px/1.6 ui-monospace,monospace}@media(max-width:650px){.wrap{padding:25px 16px}.metrics{grid-template-columns:1fr 1fr}.hero{padding-top:38px}.scenario-head{display:block}.control{display:inline-block;margin-top:15px}header{align-items:center}.version{font-size:10px}h1{letter-spacing:-1.3px}}
  </style></head><body><main class="wrap"><header><div class="wordmark">false<i>-green</i><span aria-hidden="true">_</span></div><div class="version">v${e(r.toolVersion)} / EVIDENCE REPORT</div></header><section class="hero"><p class="eyebrow">NEGATIVE CONTROLS FOR BROWSER TESTS</p><h1>Green tests.<br><em>Broken product?</em></h1><p class="lead">${e(r.suite)}. The same journey, controlled faults, repeated checks. An armed fault is not evidence until it actually applies.</p></section><div class="metrics"><div class="metric"><b class="mint">${s.detected}</b><span>DETECTED</span></div><div class="metric"><b class="amber">${s.survived}</b><span>SURVIVED</span></div><div class="metric"><b>${s.unexercised+s.unresolved}</b><span>NOT MEASURABLE</span></div><div class="metric"><b>${rate}</b><span>DETECTED / ELIGIBLE (${s.eligible}/${s.total})</span></div></div><aside class="notice"><strong>${s.exitCode === 0 ? 'All measured faults detected.' : s.exitCode === 1 ? 'False greens reproduced.' : 'Experiment incomplete.'}</strong> ${s.exitCode===2?'Resolve invalid, unexercised or unstable cases before drawing a conclusion.':'This is sensitivity to the configured faults, not a product quality or coverage score.'}</aside>${rows}<footer><p>Built by Ixequi Luna · No analytics, external assets, or remote report uploads.</p><p>${e(r.environment.node)} · ${e(r.environment.platform)} · Chromium ${e(r.environment.browser)} · Playwright ${e(r.environment.playwright)}</p><p>${e(r.createdAt)} · ${r.repetitions} repetitions per phase · ${(r.durationMs/1000).toFixed(1)} s · exit ${s.exitCode}</p><p class="hash">Suite file SHA-256: ${e(r.suiteFileSha256 ?? 'not provided (programmatic run)')}. This hashes the entry file, not its dependencies or the deployed app.</p><p>Equivalent or intentionally tolerated faults require human review. Fresh browser contexts do not reset databases. Use only disposable, authorized targets.</p></footer></main></body></html>`;
}
export async function writeReport(report: Report, directory: string): Promise<string> {
  const output = resolve(directory);
  await mkdir(output, { recursive: true });
  await Promise.all([
    writeFile(resolve(output, 'report.json'), JSON.stringify(report, null, 2) + '\n'),
    writeFile(resolve(output, 'index.html'), renderHtml(report)),
    writeFile(resolve(output, 'summary.md'), renderMarkdown(report)),
  ]);
  return output;
}
